package ex_14052025;

public class Lab132 {

   //If the values are assigned to in stance variable it'll be common to all the real workld entities.//
   //Never assign values to instance variable, as it will assign the same value that is assigned for one student to all the students in the class.
   //to avoid the above confusion, we are using constructor, so that each real world entities are assigned with unique values.
    }
}
